<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Cell background color -->
    <stroke
        android:width="1dp"
        android:color="@android:color/white" /> <!-- Border color -->
</shape>